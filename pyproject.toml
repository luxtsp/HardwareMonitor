[project]
name = "HardwareMonitor"
version = "0.1.0"
description = "A basic web monitor for your linux server"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.3",
    "psutil>=7.2.2",
    "waitress>=3.0.2",
]
