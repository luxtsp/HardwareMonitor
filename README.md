# Hardware Monitor
A simple web panel to monitor your server's temp and usage.

## How to run

to run you must have python3 installed.

I personally use UV to run the program but you could manually install the dependencies and run it with python

```
uv run main.py
```

## Web UI

For now the panel is entirely vibecoded because my frontend skills are dogshit and i was too lazy to write some shitty js code alongside it.
I'll have a friend go over it and clean it. *Exploiting the homies*

Panel is broadcasted on port 5000 and broadcasted using waitress.